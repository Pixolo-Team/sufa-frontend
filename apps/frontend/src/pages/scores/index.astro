---
// LAYOUTS //
import OperationsLayout from "@/layouts/OperationsLayout.astro";

// COMPONENTS //
import ScoresApp from "@/components/predictions/ScoresApp";
---

<OperationsLayout title="Total Scores | Skorost United">
	{/*
		`client:only` so the PIN gate decides what to render before anything paints -
		a hydrated island would flash the tools before the gate ran.
	*/}
	<ScoresApp client:only="react" />
</OperationsLayout>
