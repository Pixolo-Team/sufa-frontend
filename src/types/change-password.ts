export type ChangePasswordData = {
	password: string;
	confirmPassword: string;
};

export type ChangePasswordErrorData = {
	password: string;
	confirmPassword: string;
};

export type ChangePasswordVisibilityData = {
	password: boolean;
	confirmPassword: boolean;
};
